%YAML:1.0

#common parameters
imu_topic: "/imu0"
image_topic: "/cam0/image_raw"
output_path: "/home/pc/文档/mycode/vins_ws/src/VINS-Mono/output/2026_04_26_12_40_42/uwb"

# UWB parameters
# Recommended unified mode:
# 0: off, original VINS-Mono
# 1: UWB correction debug, external correction idea, debug only
# 2: UWB range factor mode, reserved; factor is not connected to optimization yet
uwb_fusion_mode: 0
# Compatibility/debug switches below are derived from uwb_fusion_mode.
use_uwb: 0
uwb_topic: "/uwb/range"
uwb_noise: 0.1
uwb_max_interval: 0.05
use_uwb_interpolation: 1
uwb_interp_max_gap: 0.2
use_uwb_factor: 0
use_uwb_correction: 0
uwb_correction_debug_only: 1
uwb_correction_min_anchors: 3
uwb_correction_max_norm: 2.0
uwb_anchor_positions:
  - [0.0, 0.0, 0.0]
  - [5.0, 0.0, 0.0]
  - [0.0, 5.0, 0.0]
p_uwb_imu: [0.0, 0.0, 0.0]
uwb_world_aligned: 0
uwb_world_to_vins_yaw: 0.0
uwb_world_to_vins_translation: [0.0, 0.0, 0.0]

#camera calibration 
model_type: PINHOLE
camera_name: camera
image_width: 640
image_height: 480
distortion_parameters:
   k1: -0.014831926523739894 
   k2: 0.04115487823418198
   p1: 0.0009111963856912787
   p2: -0.00027580463531532237
projection_parameters:
   fx: 405.7457739130012 
   fy: 406.24655246053493
   cx: 317.9444502041698
   cy: 242.19246296447034
# Extrinsic parameter between IMU and Camera.
estimate_extrinsic: 2   # 0  Have an accurate extrinsic parameters. We will trust the following imu^R_cam, imu^T_cam, don't change it.
                        # 1  Have an initial guess about extrinsic parameters. We will optimize around your initial guess.
                        # 2  Don't know anything about extrinsic parameters. You don't need to give R,T. We will try to calibrate it. Do some rotation movement at beginning.                        
#If you choose 0 or 1, you should write down the following matrix.
#Rotation from camera frame to imu frame, imu^R_cam
extrinsicRotation: !!opencv-matrix
   rows: 3
   cols: 3
   dt: d
   data: [0.0148655429818, -0.999880929698, 0.00414029679422,
           0.999557249008, 0.0149672133247, 0.025715529948, 
           -0.0257744366974, 0.00375618835797, 0.999660727178]
#Translation from camera frame to imu frame, imu^T_cam
extrinsicTranslation: !!opencv-matrix
   rows: 3
   cols: 1
   dt: d
   data: [-0.0216401454975,-0.064676986768, 0.00981073058949]

#feature traker paprameters
max_cnt: 150            # max feature number in feature tracking
min_dist: 30            # min distance between two features 
freq: 10                # frequence (Hz) of publish tracking result. At least 10Hz for good estimation. If set 0, the frequence will be same as raw image 
F_threshold: 1.0        # ransac threshold (pixel)
show_track: 1           # publish tracking image as topic
equalize: 1             # if image is too dark or light, trun on equalize to find enough features
fisheye: 0              # if using fisheye, trun on it. A circle mask will be loaded to remove edge noisy points

#optimization parameters
max_solver_time: 0.04  # max solver itration time (ms), to guarantee real time
max_num_iterations: 8   # max solver itrations, to guarantee real time
keyframe_parallax: 10.0 # keyframe selection threshold (pixel)

#imu parameters       The more accurate parameters you provide, the better performance
acc_n: 8.9396e-03          # accelerometer measurement noise standard deviation. #0.2   0.04
gyr_n: 4.0695e-03         # gyroscope measurement noise standard deviation.     #0.05  0.004
acc_w: 2.2498e-03         # accelerometer bias random work noise standard deviation.  #0.02
gyr_w: 1.1090e-05       # gyroscope bias random work noise standard deviation.     #4.0e-5
g_norm: 9.787561     # gravity magnitude

#loop closure parameters
loop_closure: 1                    # start loop closure
load_previous_pose_graph: 0        # load and reuse previous pose graph; load from 'pose_graph_save_path'
fast_relocalization: 0             # useful in real-time and large project
pose_graph_save_path: "/home/pc/文档/mycode/vins_ws/src/VINS-Mono/output/pose_graph/" # save and load path

#unsynchronization parameters
estimate_td: 1                      # online estimate time offset between camera and imu
td: 0.0                             # initial value of time offset. unit: s. readed image clock + td = real image clock (IMU clock)

#rolling shutter parameters
rolling_shutter: 0                  # 0: global shutter camera, 1: rolling shutter camera
rolling_shutter_tr: 0               # unit: s. rolling shutter read out time per frame (from data sheet). 

#visualization parameters
save_image: 1                   # save image in pose graph for visualization prupose; you can close this function by setting 0 
visualize_imu_forward: 0        # output imu forward propogation to achieve low latency and high frequence results
visualize_camera_size: 0.4      # size of camera marker in RVIZ
