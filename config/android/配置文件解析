配置文件各部分是什么意思



1）common parameters



imu_topic: "/imu0"
image_topic: "/cam0/image_raw"
output_path: ".../output/2026_04_16_14_05_07/reference/"

意思是：

imu_topic：VINS 从哪个 ROS 话题读 IMU 数据
image_topic：VINS 从哪个 ROS 话题读图像
output_path：结果文件输出目录，比如 vins_result_loop.csv、vins_result_no_loop.csv

这部分本身不会改变算法几何精度，但如果 topic 写错，系统就根本跑不起来。你这里的 /cam0/image_raw 和你自己的相机标定结果是一致的。

2）camera calibration



model_type: PINHOLE
camera_name: camera
image_width: 640
image_height: 480
distortion_parameters:
   k1: 0.01468
   k2: 0.22279
   p1: 0.00079
   p2: 0.00467
projection_parameters:
   fx: 506.59
   fy: 507.40
   cx: 327.51
   cy: 238.18
   
这部分的含义
model_type: PINHOLE

说明相机模型是针孔模型。
VINS 会把图像像素点，通过内参和畸变参数，转换成相机坐标系下的观测光线。

image_width / image_height

图像分辨率。必须和你实际输入图像一致。你这里是 640×480，这点是对的。

fx, fy

焦距，单位是“像素”。
它决定：

一个像素偏移，对应多大的视线角度
也就是相机视场角有多大
cx, cy

主点坐标。
意思是光轴在图像平面上的中心位置，不一定刚好在 (320, 240)。

k1, k2, p1, p2

镜头畸变参数：

k1, k2：径向畸变
p1, p2：切向畸变

它们决定图像边缘点在去畸变时怎么修正。

3）Extrinsic parameter between IMU and Camera

你当前写的是：

estimate_extrinsic: 2
extrinsicRotation: ...
extrinsicTranslation: ...
这部分的含义

这部分描述的是：

相机坐标系和 IMU 坐标系之间的刚体变换

也就是：

相机相对 IMU 怎么旋转
相机相对 IMU 在空间里偏了多少
extrinsicRotation

3×3 旋转矩阵。
表示从相机坐标系到 IMU 坐标系的旋转关系。

extrinsicTranslation

3×1 平移向量。
表示从相机原点到 IMU 原点的位移。

estimate_extrinsic

这个最重要：

0：我非常相信下面给的外参，别改它
1：我给一个初值，你在附近优化
2：我不知道外参，你自己在线估

你当前是：

estimate_extrinsic: 2

这意味着：

下面写的 extrinsicRotation / extrinsicTranslation 基本不会被当成可信真值，系统还是会在线重估。

所以你现在虽然填了外参，但由于设成了 2，本质上还是让系统自己去找外参。

4）feature tracker parameters

你当前是：

max_cnt: 150
min_dist: 30
freq: 10
F_threshold: 1.0
show_track: 1
equalize: 1
fisheye: 0
含义
max_cnt

每帧最多跟踪多少个特征点。
150 是常见值。特征太少不稳定，太多会增加计算量。

min_dist

两个特征点之间的最小像素距离。
30 表示新加特征时，不让它们太密。
值大了，点更分散；值小了，点更多但可能扎堆。

freq

前端发布跟踪结果的频率上限。
你这里是 10，意思是即使输入图像 30 Hz，也可能只按 10 Hz 往后端送。
这会影响：

每两帧之间的位移量变大
视差变大
但时间分辨率下降

如果设成 0，就是跟随原始图像频率。

F_threshold

基础矩阵 RANSAC 阈值。
越大，越容易保留匹配；越小，筛得越严格。
1.0 偏严格。

show_track

是否发布带特征点的跟踪图像。

equalize

是否做直方图均衡化。
地下车库明暗变化大，开 1 往往有帮助。

fisheye

是否是鱼眼。你这里不是。

5）optimization parameters

你当前是：

max_solver_time: 0.04
max_num_iterations: 8
keyframe_parallax: 10.0
含义
max_solver_time

每次优化允许的最长时间。

max_num_iterations

每次后端优化最多迭代几次。

keyframe_parallax

关键帧选择阈值。
视差大于这个值，更容易成为关键帧。

这部分主要影响：

实时性
优化充分程度
关键帧密度

一般不是你两次结果差异的主因。

6）imu parameters

你当前是：

acc_n: 8.9396e-03
gyr_n: 4.0695e-03
acc_w: 2.2498e-03
gyr_w: 1.1090e-05
g_norm: 9.787561
含义
acc_n

加速度计测量白噪声标准差。

gyr_n

陀螺仪测量白噪声标准差。

acc_w

加速度计 bias 随机游走噪声。

gyr_w

陀螺仪 bias 随机游走噪声。

g_norm

当地重力大小。
不是固定必须 9.81，按当地值写更严谨。

这些参数影响：

IMU 预积分置信度
视觉和 IMU 融合时谁更“被相信”
偏置估计是否稳定

如果噪声参数设得不符合你的手机真实 IMU，后端会错误分配权重。

7）loop closure parameters

你当前是：

loop_closure: 1
load_previous_pose_graph: 0
fast_relocalization: 0
pose_graph_save_path: ...
含义
loop_closure: 1

开启回环检测和位姿图优化。
这会让轨迹在回到旧区域时尝试“闭合”。

load_previous_pose_graph

是否加载之前保存的位姿图。

fast_relocalization

是否开启更快的重定位。

pose_graph_save_path

位姿图保存路径。

这一部分会直接影响：

轨迹能不能“收口”
长时间运行后漂移能不能被压回去

所以你看到 loop 比 no_loop 更合理，是很正常的。

8）unsynchronization parameters

你当前是：

estimate_td: 1
td: 0.0
含义
td

相机时间戳与 IMU 时间戳之间的时间偏移初值。

estimate_td: 1

表示在线继续估计时间偏移。

如果图像和 IMU 不严格同步，这个参数非常关键。
它影响：

图像帧对应的真实 IMU 状态
视觉和惯导是否在“同一个时刻”融合

如果 td 错得太多，会直接出现：

视觉和 IMU 对不齐
初始化困难
轨迹抖动或形变

9）rolling shutter parameters

你当前是：

rolling_shutter: 0
rolling_shutter_tr: 0
含义

表示假设你的相机是全局快门。
但手机摄像头通常其实更接近滚动快门。

如果手机有明显 rolling shutter，而你这里设成 0，会带来：

快速运动时几何畸变
转动时直线弯曲
特征匹配误差变大

不过在慢速运动下，这个问题可能还不至于成为主导问题。

10）visualization parameters

你当前是：

save_image: 1
visualize_imu_forward: 0
visualize_camera_size: 0.4
含义
save_image

是否为 pose graph 保存图像，方便回环和可视化。

visualize_imu_forward

是否显示 IMU 前向传播结果。

visualize_camera_size

RViz 里相机图标大小。

这部分主要影响显示和调试，不是轨迹几何精度主因。
